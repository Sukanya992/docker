pipeline {
    agent any 
    stages {
        stage ('Pull image from Docker Hub') {
            steps {
                script {
                    def dockerImage = 'sukanya996/ubuntu'
                    sh "docker pull ${dockerImage}"
                    sh "docker images"
                }
            }
        }
    }
}
