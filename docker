pipeline {
    agent any 
    stages {
        stage ('Pull image from dockerhub') {
            steps {
                script {
                    def dockerImage = 'sukanya/ubuntu'
                    sh "sudo docker pull ${dockerImage}"
                    sh "docker images"
                }
            }
        }
    }
}

