pipeline {
    agent any {
        stages {
            stage ('Pull image from dockerhub'){
                steps {
                    script {
                        def dockerImage = 'sukanya996/ubuntu'
                        sh "docker pull ${ubuntu}"
                    }
                }
            }
        }
    }
}
